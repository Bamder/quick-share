# QuickShare 文件闪传系统 - 配置文件示例
# 使用方法：复制此文件为 .env 并根据实际情况修改

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=0691
DB_NAME=quick_share_datagrip

# Redis 配置（可选）
# 是否启用 Redis（true/false）
# 设置为 true 时，启动服务器会自动检查并启动 Redis（如果未运行）
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
