# Python 3.12 version is required.

# 数据库相关
alembic>=1.13.0
sqlalchemy==2.0.23
pymysql==1.1.0
cryptography>=41.0.0

# Web 框架相关（FastAPI）
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# WebSocket 支持（WebRTC 信令交换需要）
websockets>=12.0
python-socketio>=5.10.0

# 数据验证和配置管理
pydantic==2.5.0
pydantic-settings==2.1.0

# Redis 支持（可选，生产环境推荐）
redis>=5.0.0

# JWT 支持
python-jose[cryptography]>=3.3.0
