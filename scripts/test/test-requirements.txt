# 测试专用依赖 - Python 3.12 兼容版本
# 包含项目运行所需的完整依赖，避免测试环境依赖缺失
# 这些版本经过验证与Python 3.12兼容

# 项目核心依赖（测试需要访问项目模块）
alembic>=1.13.0
sqlalchemy==2.0.23
pymysql==1.1.0
cryptography>=41.0.0
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets>=12.0
python-socketio>=5.10.0
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic-core>=2.14.0,<2.15.0  # 限制版本避免Python 3.13兼容性问题
redis>=5.0.0
python-jose[cryptography]>=3.3.0

# HTTP客户端 (用于FastAPI TestClient)
httpx>=0.24.0,<0.25.0

# 测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 日志增强
rich>=13.0.0

# 内存数据库测试 (SQLite已在Python标准库中)
# 如果需要更完整的数据库测试，可以添加:
# pytest-postgresql>=4.0.0  # PostgreSQL测试
# pytest-mysql>=1.0.0       # MySQL测试
